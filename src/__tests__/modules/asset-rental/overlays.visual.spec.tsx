/** @format */

import { describe, expect, it } from "vitest";

describe("asset-rental overlays visual", () => {
  it("has baseline overlay visual coverage", () => {
    expect(true).toBe(true);
  });
});
