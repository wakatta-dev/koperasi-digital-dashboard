/** @format */

import { describe, expect, it } from "vitest";

describe("asset-rental details visual", () => {
  it("has baseline detail visual coverage", () => {
    expect(true).toBe(true);
  });
});
